name: 🐳 Build and Publish

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_api:
    name: Build API
    uses: octodemo/od-octocat-supply-workflows/.github/workflows/build-publish-containers.yml@main
    if: |
      github.actor != 'dependabot[bot]' && 
      !startsWith(github.head_ref, 'dependabot/')
    with:
      service_name: api
      context_path: ./api
      dockerfile_path: ./api/Dockerfile
      registry_url:  "ghcr.io"

  build_frontend:
    name: Build Frontend
    uses: octodemo/od-octocat-supply-workflows/.github/workflows/build-publish-containers.yml@main
    if: |
      github.actor != 'dependabot[bot]' && 
      !startsWith(github.head_ref, 'dependabot/')
    with:
      service_name: frontend
      context_path: ./frontend
      dockerfile_path: ./frontend/Dockerfile
      registry_url: "ghcr.io"


  trigger_deploy:
    name: Trigger Deployment
    needs: [build_api, build_frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Trigger deployment workflow
        uses: actions/github-script@v8
        with:
          script: |
            github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'deploy.yml',
              ref: 'main',
              inputs: {
                'api_image': '${{ needs.build_api.outputs.image }}',
                'frontend_image': '${{ needs.build_frontend.outputs.image }}'
              }
            });
